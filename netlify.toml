[build]
  command = "pip install -r requirements.txt && python3 build_catalog.py && hugo --minify"
  publish = "public"

[build.environment]
  HUGO_VERSION = "0.159.0"
  PYTHON_VERSION = "3.12"

[[headers]]
  for = "/gh-download/*"
  [headers.values]
    Access-Control-Allow-Origin = "*"
